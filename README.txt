Práctica 6 - Pruebas y Herramientas de Integración Continua

El fichero debe tener el nombre 'grafo.txt' y su formato debe ser:

Debe existir un vértice inicial '1' para calcular el camino de coste mínimo hasta los demás vértices.

Para cada columna separada por tabulaciones:

Primera columna: vértice de partida.

Demás x columnas: (vértice adyacente) + (,) + (coste)

Demo: El fichero 'grafo.txt' viene con un grafo de prueba.